import React, { useState } from "react";

const Like = () => {
    [liked_users, set_liked_users] = useState([]); // liked user ids stored in a list and then sent to backend after length 0f 6
    
    
}